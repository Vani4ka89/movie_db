import React, {FC} from 'react';
import {MoviesList} from "../../components";

const MoviesPage: FC = () => {
    return (
        <div>
            <MoviesList/>
        </div>
    );
};

export {MoviesPage};